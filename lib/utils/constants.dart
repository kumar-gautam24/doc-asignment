import 'package:flutter/material.dart';

/// Animation durations
const kAnimationDuration = Duration(milliseconds: 300);

/// Default icon size
const kDefaultIconSize = 60.0;

/// Dock background color
const kDockBackgroundColor = Colors.black12;
